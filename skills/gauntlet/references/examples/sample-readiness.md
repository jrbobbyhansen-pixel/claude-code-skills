# READINESS — gauntlet

**Goal:** payments live + hand off to the team  **Deadline:** 2026-05-29  **Mode:** deep

## VERDICT: **NO-GO**  (ship-confidence 64%) — 2 P0 + 1 UNPROVEN critical path between you and ship.

_P0: 2 (▼ from 5)   ship-confidence: 64% (▲ from 41)_
_Re-opened (files changed since proven-green): src/auth/session.ts_

---

## Sections (worst-first)

- 🔴 **§payments** — 38/100 · 2 P0 · 1 unproven-crit · 9 findings
- 🟡 **§auth** — 72/100 · 0 P0 · 0 unproven-crit · 4 findings · ⚠ re-opened
- 🟡 **§data** — 80/100 · 0 P0 · 0 unproven-crit · 3 findings
- 🟢 **§core** — 91/100 · 0 P0 · 0 unproven-crit · 2 findings
- 🟢 **§marketing-ui** — 96/100 · 0 P0 · 0 unproven-crit · 1 finding

---

## P0 — BLOCKS HANDOFF (2)

**MONEY-001** `[$][DATA]` `src/api/webhooks/stripe.ts:88` — webhook handler is not idempotent; Stripe retries on timeout → **double-charge**.
- **FIX:** add a unique constraint on `processed_events(event_id)` and short-circuit if the event was already handled (diff below).
- **evidence:** PROVEN — Field-Test ran `stripe trigger payment_intent.succeeded` ×2 → 2 order rows.
- **interaction:** Money's retrying webhook × Data's non-idempotent `orders.insert` (Data filed neither alone).

**SEC-003** `[SEC]` `supabase/migrations/0007_orders.sql:1` — `orders` table has **RLS disabled**; any anon key can read every customer's order.
- **FIX:** `alter table orders enable row level security;` + policy `using (auth.uid() = user_id)`.
- **evidence:** PROVEN — Field-Test probed via Supabase MCP with the anon key → returned 1,284 rows.

## UNPROVEN — critical path (scored as P0-equivalent) (1)

**LIVE-002** `[LIVE]` `src/email/send.ts:40` — magic-link deliverability **UNPROVEN**: DKIM record present but inbox placement not confirmed.
- **recipe:** `dig +short TXT resend._domainkey.<domain>` → present; provider status `delivered`.
- **gap:** `[USER MUST RUN]` — confirm the magic-link lands in **inbox, not spam**, in a real mailbox. Until then this blocks "team can log in unaided."

## P1 — SHOULD FIX (5)
`REL-004` `src/api/webhooks/stripe.ts:61` no timeout on the downstream grant call → one slow dependency hangs the handler. `XFER-002` `.env.example` missing `RESEND_API_KEY`, `STRIPE_WEBHOOK_SECRET` — a fresh clone can't boot. …

## P2 — PARKED, post-goal (7)
Phase-2 analytics dashboard · legacy tier-pricing code (unreached) · …

## CUT — Razor delete-list (−412 LOC)
- `src/legacy/oldCheckout.ts` (0 callers, grep-verified) — `git rm`. −180 LOC
- `src/utils/format.ts:30-74` duplicate of `money.ts:12` — unify. −44 LOC
- 3 unused deps (`moment`, `lodash.merge`, `uuidv1`) — `npm uninstall`. −188 LOC (bundle)

## LIVE TESTS — 6 run, 2 [USER MUST RUN]
payments idempotency ✅ PROVEN · RLS anon-probe ✅ PROVEN · magic-link deliverability ⚠ [USER MUST RUN] · live-mode real card ⚠ [USER MUST RUN] · cron registered ✅ · DNS/TLS ✅

## DELTA vs last run
P0 5 → 2 (▼) · ship-confidence 41% → 64% (▲) · re-opened: `src/auth/session.ts` (changed since proven-green) · newly GREEN: §core, §marketing-ui

---
_Generated by `aggregate.py`. Fix the 2 P0 + run the 2 `[USER MUST RUN]` tests, then re-run `/gauntlet --deep` — GO requires zero P0, zero UNPROVEN-crit, and ship-confidence ≥ 80._
